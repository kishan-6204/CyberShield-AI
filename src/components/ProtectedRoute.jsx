// Day 1 placeholder: this component preserves a scalable route boundary without enforcing authentication yet.
function ProtectedRoute({ children }) {
  return children;
}

export default ProtectedRoute;
